#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void process_route_sequence(char* sequence);
char* update_route_sequence(char* sequence);
void forward_next_hop(char *sequence);
void forward_instruction(char* instruction);
int is_target(char* agent_name);


