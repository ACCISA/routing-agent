#include "../Router/router.h"

int
parse_routing_message(unsigned char* data, unsigned char* header_data, unsigned char* payload_data)
{
}

rheader_t*
create_routing_header(unsigned char* data)
{
}

rpayload_t*
create_routing_payload(unsigned char* data)
{
}
